<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/svScroll"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/semi_trans_bg"
    android:orientation="vertical" >

    <LinearLayout 
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_gravity="center">    
    
        <include android:id="@+id/stage1" layout="@layout/activity_tour_simple_element" />
        <include android:id="@+id/stage2" layout="@layout/activity_tour_simple_element" />
        <include android:id="@+id/stage3" layout="@layout/activity_tour_simple_element" />
        <include android:id="@+id/stage4" layout="@layout/activity_tour_simple_element" android:visibility="gone"/>

        <Button
                android:id="@+id/btnCloseTut"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:background="@drawable/btn_bg_green"
                android:layout_marginTop="50dp"
                android:layout_marginRight="16dp"
                android:layout_marginLeft="16dp"
                android:paddingEnd="20dp"
                android:paddingStart="20dp"
                android:text="@string/tour_new_title4"
                android:drawableRight="@drawable/btn_end_tut"
                android:textColor="@android:color/white"
                android:visibility="gone"/>
        
    </LinearLayout>

</ScrollView>